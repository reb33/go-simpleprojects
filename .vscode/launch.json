{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CLI",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}", // или путь к main.go
            // "args": ["avg", "--flag", "value"], // Аргументы команды
            "console": "integratedTerminal" 
        }
    ],
    
}